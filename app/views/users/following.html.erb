<% @title = "Movies from people you're following" %>
<article>

  <% if not @movies.empty? %>
    <h1>People you're following watched <%= count @movies %>.</h1>
    <nav><p>
      <%= raw current_user.friends.map { |friend| link_to friend.name, watched_path(friend) }.join ', ' %>
    </p></nav>
    
    <%= render 'movies/paginated', :movies => @movies %>
  <% else %>
    <% if my_page? %>
      <section class="intro">
        <p>
          When someone of your Twitter or Facebook connections uses this site,<br>
          movies they watched will be listed here.
        </p>
      </section>
    <% end %>
    
    <p>No movies found.</p>
  <% end %>

</article>
