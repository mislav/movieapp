<!DOCTYPE html>
<html class="no-js">
  <head>
    <meta charset="UTF-8" />
    <%= csrf_meta_tag %>
    <title>Movies app!</title>
    <%= stylesheet_link_tag "movie" %>
  </head>
  
  <body class="<%= controller.action_name %>">
    <%= javascript_include_tag 'modernizr' %>
    
    <header>
      <h1><a href="/">The Movie App</a></h1>
    
      <nav>
        <% if logged_in? %>
          <p>
            Logged in as <%= link_to(current_user.username, watched_path(current_user)) %>,
            <%= link_to 'logout', logout_path %>
          </p>
        <% elsif Movies.offline? %>
          <%= link_to 'Instalogin!', instant_login_path %>
        <% else %>
          Login with
          <%= link_to 'Twitter', twitter_login_path %> or
          <%= link_to 'Facebook', facebook_login_path %>
        <% end %>
      </nav>
    </header>
    
    <% if flash[:warning] %>
      <p class="warning"><%= flash[:warning] %></p>
    <% end %>
      
    <%= form_tag root_path, :method => 'get' do %>
      <div>
        <%= label_tag :q, 'Movie search:' %>
        <%= text_field_tag :q, @query, :type => 'search', :results => '5' %>
        <%= submit_tag "search", :name => nil %>
      </div>
    <% end %>
      
    <%= yield %>
    
    <%= javascript_include_tag 'prototype', 'rails', 'application' %>
  </body>

</html>
