<!DOCTYPE html>
<html>
  <head>
    <title>Movies app!</title>
    <%= stylesheet_link_tag "main" %>
  </head>
  <body>
    <ol id="session-status" class="actions">
      <% if logged_in? %>
        <li>Logged in as <%= link_to(current_user.username, watchlist_path(:username => current_user.username)) %></li>
        <li><%= link_to 'logout', logout_path %></li>
      <% else %>
        <li><%= link_to 'login', '/login' %></li>
      <% end %>
    </ol>
    
    <%= yield %>
  </body>
</html>
